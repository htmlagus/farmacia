<?php require 'templates/layout/header.phtml' ?>
<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <title>Detalle del Cliente</title>
</head>

<body>
    <div class="container mt-5">
        <?php if (isset($cliente) && $cliente): ?> <!-- Verificacion para controlar que $cliente no sea nulo -->
            <h1>Detalle del Cliente: <?= htmlspecialchars($cliente->nombre) ?></h1>
            <ul class="list-group">
                <li class="list-group-item"><strong>ID:</strong> <?= htmlspecialchars($cliente->cliente_id) ?></li>
                <li class="list-group-item"><strong>Nombre:</strong> <?= htmlspecialchars($cliente->nombre) ?></li>
                <li class="list-group-item"><strong>Apellido:</strong> <?= htmlspecialchars($cliente->apellido) ?></li>
                <li class="list-group-item"><strong>DNI:</strong> <?= htmlspecialchars($cliente->dni) ?></li>
                <li class="list-group-item"><strong>Obra Social:</strong> <?= htmlspecialchars($cliente->obra_social) ?></li>
            </ul>
            <a href="<?= BASE_URL ?>clientes" class="btn btn-secondary mt-3">Volver al listado</a>
        <?php else: ?>
            <!-- Manejo de errores si es que el cliente no se encuentra -->
            <h1>Cliente no encontrado</h1>
            <p>Lo sentimos, no se pudo encontrar el cliente solicitado.</p>
            <a href="clientes" class="btn btn-secondary mt-3">Volver al listado</a>
        <?php endif; ?>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>